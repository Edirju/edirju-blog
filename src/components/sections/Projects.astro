---
import Section from "../Section.astro";
import { basics, projects } from "@cv";
import {
  Rss,
  Github,
  Html5,
  Css3,
  React,
  Javascript,
  Python,
  Vite,
} from "simple-icons-astro";
import Circle from "@/icons/circle.astro";
import AstroIcon from "@/icons/AstroIcon.astro";
import Data from "@/icons/Data.astro";
const SKILL_ICONS: Record<string, any> = {
  Html5,
  Css3,
  React,
  Javascript,
  AstroIcon,
  Vite,
};
---

<Section>
  <article class="suscribe-container mt-6">
    <p>
      Los pequeños detalles marcan una gran diferencia. Con los años he
      aprendido a fijarme en esos detalles. Nunca dejo de aprender. He aquí
      varias recopilaciones a lo largo de este camino.
    </p>
    <!-- <div class="box-left">
      <a href="#" class="rss-btn">
        <Rss class="h-3 w-3" />
         <span>Suscríbete</span> 
      </a>
    </div> -->
  </article>

  <ul class="projects-items">
    {
      projects.map(
        ({ url, description, highlights, name, isActive, github }) => {
          return (
            <li>
              <article>
                <header>
                  <h3>
                    <a
                      href={url}
                      title={`Ver el proyecto ${name}`}
                      target="_blank"                      
                    >
                      {name}
                    </a>
                    {isActive && (
                      <span>
                        <Circle />
                      </span>
                    )}
                    {github && (
                      <a
                        class="github-code-link"
                        href={github}
                        target="_blank"
                        title={`Ver código fuente del proyecto ${name}`}
                      >
                        <Github class="w-4 h-4" />
                      </a>
                    )}
                  </h3>
                  <p>
                    {description}
                  </p>
                </header>
                <footer>
                  <ul>
                    {highlights.map((highlight) => {
                      return (
                        <span>
                          {highlight}
                        </span>
                      );
                    })}
                  </ul>
                </footer>
              </article>
            </li>
          );
        }
      )
    }
  </ul>
</Section>

<style>
  .projects-items {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 1.5rem;
  }
  li {    
    padding: .5rem;
    border-radius: .5rem;
    position: relative;
    overflow: hidden;
    transition: all 0.3s ease-in-out;
  }
  li:hover {
    background-color: rgb(var(--text-100), 0.03);
  }
  article {
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    gap: 0.25rem;
    height: 100%;
  }
  article header {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    margin-bottom: 4px;
  }
  article header h3{
    display: flex;
    align-items: center;
    gap: 1rem;
    border-left: 2px solid rgb(var(--text-100), 0.45);
    padding-left: 8px;
    font-size: 14px;
    font-weight: 600;
    text-wrap: pretty;
    letter-spacing: -0.05rem;
    color: rgb(var(--text-200), 0.85);
    transition: all 0.3s ease-in-out;
  }
  article header h3:hover {
    text-decoration: underline;
    text-underline-offset: 4px;
  }
  article header h3 span {
    color: rgb(0, 188, 47, 1);
  }
  article header h3 .github-code-link {
    color: rgb(var(--text-100), 0.65);
  }
  article header h3 .github-code-link:hover {
    color: rgb(var(--text-100), 0.85);
  }
  article header p {
    font-size: 12px;
    line-height: 1.4;
    color: rgb(var(--text-100), 0.45);
    text-wrap: pretty;
    transition: all 0.3s ease-in-out;
  }
  article footer ul {
    display: flex;
    flex-wrap: wrap;
    gap: 4px;
    font-size: 10px;    
    color: rgb(var(--secondary-100), 0.5);
  }
  article footer ul span {
    background: rgb(var(--text-200), 0.08);
    padding: 1px 4px;
    border-radius: 4px;
    width: fit-content;
  }  
</style>
